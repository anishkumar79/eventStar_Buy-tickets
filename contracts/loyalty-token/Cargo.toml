[package]
name = "loyalty-token"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "20.8.2"

[dev-dependencies]
soroban-sdk = { version = "20.8.2", features = ["testutils"] }
